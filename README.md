# 🏇 horse-racing-game-js

A browser-based horse racing board game built with vanilla JavaScript.

## 🎮 Demo

[Play here](https://rokujyouhitoma.github.io/horse-racing-game-js/)

## Overview

A digital implementation of a horse racing card game. Players use play cards (Step, Rank, Dash) to advance their horses along the racetrack. The game features a custom JavaScript engine with a main game loop, scene management, an event system, and a seeded random number generator.

## Features

- **Game Engine** — Fixed-interval update loop with variable-rate rendering and FPS display
- **Scene Management** — Scene/SceneDirector system with OnEnter/OnExit/OnPause/OnResume lifecycle hooks
- **Event System** — DOM Level 2–inspired event system (Event, EventTarget, EventListener)
- **Play Cards** — Three card types: Step (advance a horse), Rank (adjust position), Dash (special move)
- **Master Data** — Structured master data with a relationship checker and stub loader
- **Value Validation** — Boot-time master data validation via `ValueChecker` (min, nonEmpty, regex, in)
- **Seeded RNG** — Xorshift-based random number generator for reproducible randomness
- **Template Engine** — JavaScript port of Python's Tornado template engine for HTML rendering
- **Undo Support** — Undo the last played card during a race
- **Debug Toggle (Hamburger Menu)** — A top-right "≡" button that hides/shows the in-game Debug Menu for scene and race control
- **Pub/Sub Architecture** — Decoupled renderer/model communication via `Events.Race.OnChanged`

## Project Structure

```
horse-racing-game-js/
├── docs/                          # Project management, requirements, technical designs, and ADRs
│   ├── adr/                       # Architectural Decision Records (ADR-01 through ADR-06)
│   ├── issue/                     # Issue tracking documents (ISSUE-01 through ISSUE-11)
│   ├── GDD-01-game_design_document.md   # Game Design Document
│   ├── MNG-01-document_ledger.md  # Document ledger & naming rules
│   ├── MNG-02-development_process.md    # Development process & history log
│   ├── MNG-03-problem_management.md     # Problem management & issue tracker
│   ├── MNG-04-test_plan.md        # Software test plan
│   ├── MNG-05-execplan_format.md  # Execution plan format definition
│   ├── REQ-01-user_requirements.md      # User requirements
│   ├── REQ-02-feature_list.md     # Feature list
│   ├── REQ-03-system_requirements.md    # System requirements
│   ├── DSN-01-high_level_design.md      # High-level architecture design
│   ├── DSN-02-low_level_design.md       # Low-level design
│   └── USR-01-user_manual.md      # Official rulebook & user guide
├── src/
│   ├── css/
│   │   └── main.css               # Main stylesheet (HSL color variable management)
│   └── js/
│       ├── lib/                   # General-purpose library layer (game-agnostic)
│       │   ├── xorshift.js        # Seeded random number generator
│       │   ├── event.js           # Custom event system (ExEvent)
│       │   ├── locator.js         # Service locator
│       │   ├── router.js          # URL hash-based router
│       │   ├── template.js        # Tornado-compatible template engine
│       │   └── template_test.js   # Unit test suite (run via make test)
│       └── game/                  # Game-specific logic layer
│           ├── lib/               # Game engine foundation
│           │   ├── engine.js      # Main game loop
│           │   ├── scene.js       # Scene management
│           │   ├── repository.js  # Data repository
│           │   ├── publisher.js   # Pub/Sub event bus
│           │   └── command.js     # Command pattern (Undo/Redo)
│           ├── layers/            # UI layers (View layer)
│           ├── renderers/         # Rendering components
│           ├── events.js          # Game-specific event definitions
│           ├── templates.js       # HTML template definitions
│           ├── entities.js        # Game entities (Race, Racetrack, etc.)
│           ├── checker.js         # Master data validation (RelationshipChecker, ValueChecker)
│           └── main.js            # Game main & entry point
├── tools/                         # Build tools (binaries)
│   ├── closure-compiler/          # Google Closure Compiler
│   └── closure-stylesheets/       # Google Closure Stylesheets
├── index.html                     # Development entry point (reads source files directly)
├── compiled.html                  # Production entry point (uses minified JS)
├── main-min.js                    # [Generated] Compiled JS (generated by make)
├── main-min.css                   # [Generated] Compiled CSS (generated by make)
├── Makefile                       # Build configuration
├── CHANGELOG.md                   # Changelog
└── CONTRIBUTING.md                # Contribution guidelines
```

## Getting Started

### Prerequisites

| Tool | Version | Purpose |
| :--- | :--- | :--- |
| **Java** | 11+ | Running Google Closure Compiler / Closure Stylesheets |
| **Node.js** | 18+ | Running tests via `make test` |
| **Git** | 2.x+ | Version control |
| **Modern browser** | Chrome / Edge / Firefox / Safari (latest) | Playing & debugging |

> **Note:** npm, webpack, and other frontend build tools are **not required**. This project is built with Vanilla JS and the `Makefile` alone handles the full build.

### Running Locally

Open `index.html` directly in a browser, or serve it with any static file server:

```bash
# Clone the repository
git clone https://github.com/rokujyouhitoma/horse-racing-game-js.git
cd horse-racing-game-js

# Serve with Python (no install required)
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

### Building

```bash
make
```

The compiled output will be written to `main-min.js` and `main-min.css`. Open `compiled.html` in a browser to play the compiled version.

### Running Tests

```bash
make test
```

Runs the unit test suite via Node.js and prints a PASS/FAIL summary to the console.

## Game Rules

- Each player controls a horse on the racetrack
- On your turn, draw a card from the deck and add it to your hand
- Play one card from your hand to apply its effect
  - **Step cards** advance a horse by a fixed number of steps
  - **Rank cards** adjust the position of a horse by rank
  - **Dash cards** apply special movement effects
- The race ends when finishing conditions are met

## Architecture

The engine follows a component-based design:

| Component | Description |
| :--- | :--- |
| `Engine` | Main loop, fixed-interval update / variable-rate render cycle |
| `SceneDirector` | Manages scene transitions and lifecycle state |
| `Repository` | Data query system (find-by-id style) |
| `Locator` | Service locator for singleton instances |
| `Publisher` | Observer pattern for decoupled communication |
| `Router / Matcher` | URL hash-based dispatcher with `window.history` support |
| `Template` | Lightweight HTML template engine (Tornado-compatible syntax) |
| `Checker` | Boot-time data integrity validation (`RelationshipChecker`, `ValueChecker`) |

## Development

All design decisions, requirements, and issue tracking are documented under [`docs/`](docs/):

| Document | Description |
| :--- | :--- |
| [GDD-01](docs/GDD-01-game_design_document.md) | Game Design Document |
| [MNG-01](docs/MNG-01-document_ledger.md) | Document ledger & naming rules |
| [MNG-02](docs/MNG-02-development_process.md) | Development process, policies & history log |
| [MNG-03](docs/MNG-03-problem_management.md) | Issue tracker |
| [MNG-04](docs/MNG-04-test_plan.md) | Software test plan |
| [MNG-05](docs/MNG-05-execplan_format.md) | Execution plan format definition |
| [DSN-01](docs/DSN-01-high_level_design.md) | High-level architecture |
| [DSN-02](docs/DSN-02-low_level_design.md) | Low-level design |
| [ADR-01–06](docs/adr/) | Architecture Decision Records |

For contribution guidelines (coding standards, branch strategy, PR rules), see [CONTRIBUTING.md](CONTRIBUTING.md).

## Changelog

See [CHANGELOG.md](CHANGELOG.md) for a full history of notable changes.

## License

See repository for details.
